/// This is an empty ass script that will hopefully stop things from crashing
/// because hopefully it will have script ID 0, so if someone doesn't have a script defined
/// and a script_execute is called, it'll call this empty ass script. 