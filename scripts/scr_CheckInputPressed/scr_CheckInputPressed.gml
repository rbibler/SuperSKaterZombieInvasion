/// This is a simple script that checks if an input was pressed based on things.
/// @arg inputToCheck
/// @arg isItThisInput

var inputToCheck = argument0;
var isItThisInput = argument1;

return inputToCheck & isItThisInput;